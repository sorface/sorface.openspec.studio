## 1. Модель каскада

- [x] 1.1 Добавить типы и чистые функции состояния каскада `specs → design → tasks`, включая alias `spec/specs`, переходы complete/interrupted и цели agent для каждого этапа.
- [x] 1.2 Покрыть модель unit-тестами для успешных переходов, несовпадающей операции, остановки и инструкции сохранения актуальных `[x]` задач.

## 2. Управление OpenSpec-операциями

- [x] 2.1 Расширить workflow controller методом запуска каскада и наблюдаемым состоянием текущего/завершённых этапов.
- [x] 2.2 После записи текущего draft получать свежий status и автоматически запускать следующий артефакт; при reject, cancel, failure или ошибке запуска безопасно прерывать каскад.
- [x] 2.3 Сохранить прежнее поведение одиночных действий design/tasks и обновление дерева/файлов после каждой записи.

## 3. Предупреждение и прогресс UI

- [x] 3.1 Добавить в toolbar `proposal.md` модальное предупреждение при существующем `tasks.md` с перечнем затрагиваемых артефактов и действиями «Отмена», «Создать новый change», «Принять риск и обновить».
- [x] 3.2 Подключить переход в существующий мастер нового change и запуск каскада только после явного подтверждения.
- [x] 3.3 Показать текущий этап, остановку и завершение каскада в review-диалоге, не смешивая пересогласование планов с progress выполненных задач.
- [x] 3.4 Добавить адаптивные стили, focus-visible состояния и доступные названия modal/controls.

## 4. Проверка

- [x] 4.1 Обновить integration/source-contract тесты для warning modal, каскадного controller flow и правила завершённости по `tasks.md`.
- [x] 4.2 Выполнить `npm run check`, релевантную сборку и browser-проверку сценариев подтверждения, отмены и перехода к новому change.
- [x] 4.3 Оформить действие «Просмотреть результат» в панели операций как полноценную стабильную кнопку с hover/focus-состояниями и проверить её source-contract тестом.
- [x] 4.4 Стилизовать кнопку записи принятого draft в Store как основное действие с устойчивыми hover/focus-состояниями и покрыть source-contract тестом.
- [x] 4.5 Удалить из running-state review-диалога подпись «реальные файлы не изменяются», сохранив таймер операции, и закрепить это source-contract тестом.
- [x] 4.6 Перенести время выполнения в одну строку с текущим статусом agent, оформить таймер справа и удалить отдельную строку «Прошло…».
- [x] 4.7 Добавить чистую line-diff модель, выравнивающую неизменённые, заменённые, добавленные и удалённые строки, и покрыть её unit-тестами.
- [x] 4.8 Заменить raw textarea-предпросмотр на синхронный split-diff с номерами строк, красной/зелёной подсветкой, счётчиками изменений и browser-проверкой.
- [x] 4.9 Собрать статус принятого draft в отдельную компактную карточку, выровнять её справа, отделить пояснение от основного действия и проверить адаптивность в browser.
- [x] 4.10 Установить размер текста Markdown в обеих колонках split diff равным `13px` и проверить вычисленное значение в browser.
- [x] 4.11 Убрать из split diff номера строк и маркеры `+`/`−`, форматировать Markdown-блоки и inline-разметку в обеих колонках и проверить результат тестами и в browser.
- [x] 4.12 Оформить заключение agent как отдельную Markdown-карточку с абзацами, списками и inline-code вместо сплошного текста, затем проверить тестами и в browser.
- [x] 4.13 Не показывать нижнюю summary-карточку выбранной операции после статуса `accepted`, сохранив запись в истории и просмотр для операций, которые ещё требуют внимания.
- [x] 4.14 Переименовать правую панель в «История операций» и переработать свёрнутый rail: разнести иконку, вертикальную подпись и счётчик, добавить hover/focus-состояния и проверить оба вида в browser.
- [x] 4.15 Уменьшить Markdown-текст split diff до `12px` и сделать заголовок каждого изменённого файла кнопкой сворачивания с chevron, `aria-expanded` и сохранением счётчиков в закрытом состоянии.
- [x] 4.16 Добавить debounce-автосохранение редактируемого Markdown-файла через `3` секунды после последнего изменения, не запускать его для read-only, conflict или уже сохраняемого документа и сохранить ручную запись.
- [x] 4.17 Не показывать в toolbar `proposal.md` действие обновления уже существующего `design.md`; оставлять только «Создать design.md» до создания файла и обновлять существующий design через каскад specs.
- [x] 4.18 Запускать существующий каскад при перегенерации specs, если уже существует `design.md` или `tasks.md`: обновлять `specs → design` для design-only change и `specs → design → tasks` при существующем tasks, не создавая отсутствующий downstream-артефакт.
- [x] 4.19 Включить текущий `proposal.md` в review первого этапа «Перегенерировать diff specs»: разрешить ограниченную mutation scope для `proposal.md` и delta specs, дать agent явную цель их синхронизации и обновить текущий редактор после принятой записи.
- [x] 4.20 Переименовать действие повторного обновления из `proposal.md` в «Обновить», сохранив существующую иконку, accessible name и проверку presentation-состояния.
- [x] 4.21 Отображать выбранный Heading 1–6 в heading-селекторе toolbar жирным шрифтом, оставляя Paragraph обычным; синхронизировать класс состояния и покрыть lifecycle/CSS тестами.
- [x] 4.22 Добавить безопасный backend `git blame` для выбранного Markdown-файла и двухвкладочную read-only панель «Аннотации / Коммиты» с диапазонами строк, фрагментами, автором, датой, сообщением, hash, локальными состояниями и автоматическими/browser-тестами.
- [ ] 4.23 Переработать вкладку аннотаций по IDE-примеру в широкую построчную таблицу `дата | автор | номер | Markdown`, сохранить commit-контекст и локальную подсветку, покрыть разворачивание диапазонов unit/source-contract тестами и проверить компоновку в browser.
- [x] 4.24 Унифицировать кнопку обновления для `proposal.md`, `design.md` и `tasks.md` и запускать каскад только по существующим зависимым planning-артефактам.
- [x] 4.25 Визуально различать операции `proposal`, `specs`, `design` и `tasks` в истории устойчивыми цветом и символом маркерами, сохранив минималистичный вид и доступное название.
- [x] 4.26 Удалить предварительное модальное подтверждение обновления planning-артефактов и запускать рассчитанный каскад сразу после сохранения текущего документа, сохранив review-before-write.
- [x] 4.27 При создании change заранее показывать полный маршрут `proposal.md → diff specs → design.md → tasks.md` и после записи proposal автоматически запускать последовательную подготовку остальных planning-артефактов.
- [x] 4.28 Удалить отдельную нижнюю summary-карточку текущей операции и открывать её результат только по нажатию на строку операции в истории, не сбрасывая live-прогресс.
- [x] 4.29 Автоматически принимать и записывать корректный planning-результат без файловых изменений, не открывать для него пустой review и продолжать каскад, сохранив явное принятие любого непустого diff.
